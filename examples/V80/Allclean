#!/bin/sh
cd ${0%/*} || exit 1                        # Run from this directory
. $WM_PROJECT_DIR/bin/tools/CleanFunctions  # Tutorial clean functions

cleanCase

# Remove decomposeParDict
rm -r logs/

# Remove some of the farm2d generated files
rm *.pdf
rm *.png
rm turbines.dat
#------------------------------------------------------------------------------
